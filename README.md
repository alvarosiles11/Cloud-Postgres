# cloud-MongoDB
* step cd  ./postgres.com

* 1ro sudo chmod u+r+x deploy.sh
* 2do sudo ./deploy.sh

* 3ro 
sudo ufw enable
sudo ufw app list
sudo ufw allow 'OpenSSH'
sudo ufw allow 5432
sudo ufw status
